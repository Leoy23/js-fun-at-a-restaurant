function nameMenuItem(items) {
return `Delicious ${items}`
}

function createMenuItem(name, price, type) {
  name: "French Toast",
  price: 10.99,
  type:"breakfast",
  }
}



module.exports = {
  nameMenuItem,
  createMenuItem,
  // addIngredients,
  // formatPrice,
  // decreasePrice,
  // createRecipe
}
